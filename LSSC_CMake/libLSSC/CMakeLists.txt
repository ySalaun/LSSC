CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(libLSSC)

SET(SRC LibLSSC.h LibLSSC.cpp utilities.h utilities.cpp)

ADD_LIBRARY(LSSC ${SRC})

INCLUDE_DIRECTORIES(..)		
	
INSTALL(FILES CMakeLists.txt ${SRC} DESTINATION src/libLSSC)
